Include files which are shared between C and assembly

Please take care, that the syntax matches between the two languages.

Therefore comments are (unfortunately) not allowed - only preprocessor
statements like #if or #define are working
